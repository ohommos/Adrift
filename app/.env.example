# Point this at the machine running `npm run server:dev`, reachable from
# your phone (same Wi-Fi). "localhost" only works in a simulator on the
# same machine as the server — a real device needs your computer's LAN IP,
# e.g. http://192.168.1.23:4000
EXPO_PUBLIC_API_URL=http://localhost:4000
