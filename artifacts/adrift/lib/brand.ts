/**
 * The app's standing line. Kept in one place because it appears in the two
 * spots where someone is asking what Adrift is — the first screen and the
 * bottom of their own profile — and those two must never drift apart.
 */
export const TAGLINE = 'An honest human connection';
